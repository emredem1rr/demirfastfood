<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ürünü Düzenle</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-5">
        <h1>Ürünü Düzenle</h1>
        <form action="/admin/edit/<%= product.id %>" method="POST">
            <div class="mb-3">
                <label for="isim" class="form-label">Ürün İsmi</label>
                <input type="text" class="form-control" id="isim" name="isim" value="<%= product.isim %>" required>
            </div>
            <div class="mb-3">
                <label for="kategori" class="form-label">Kategori</label>
                <select class="form-select" id="kategori" name="kategori" required>
                    <option value="URUN" <%= product.kategori === 'URUN' ? 'selected' : '' %>>URUN</option>
                    <option value="MENU" <%= product.kategori === 'MENU' ? 'selected' : '' %>>MENU</option>
                    <option value="ICECEK" <%= product.kategori === 'ICECEK' ? 'selected' : '' %>>ICECEK</option>
                </select>
            </div>
            <div class="mb-3">
                <label for="fiyat" class="form-label">Fiyat</label>
                <input type="number" step="0.01" class="form-control" id="fiyat" name="fiyat" value="<%= product.fiyat %>" required>
            </div>
            <div class="mb-3">
                <label for="eski_fiyat" class="form-label">Eski Fiyat</label>
                <input type="number" step="0.01" class="form-control" id="eski_fiyat" name="eski_fiyat" value="<%= product.eski_fiyat || '' %>">
            </div>
            <div class="mb-3">
                <label for="gramaj" class="form-label">Gramaj</label>
                <input type="number" class="form-control" id="gramaj" name="gramaj" value="<%= product.gramaj %>" required>
            </div>
            <div class="mb-3">
                <label for="kalori" class="form-label">Kalori</label>
                <input type="number" class="form-control" id="kalori" name="kalori" value="<%= product.kalori %>" required>
            </div>
            <div class="mb-3">
                <label for="resim">Resim</label>
                <br>
                <input
                  type="file"
                  id="resimyolu"
                  name="resimyolu"
                  accept="image/*"
                  style="width: 935px"
                />
              </div>
            <button type="submit" class="btn btn-primary">Kaydet</button>
            <a href="/admin" class="btn btn-secondary">İptal</a>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
